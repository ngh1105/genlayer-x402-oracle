{
  "name": "x402-paywalled-data-oracle-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal genlayer-js client stub for the x402 Paywalled-Data Oracle. Submits a query and reads the resolved result.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/client.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "genlayer-js": "^1.1.8"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsx": "^4.16.0",
    "@types/node": "^20.14.0"
  }
}
